# dependencies
node_modules/
/.pnp
.pnp.js

# next.js
.next/
out/

# production
build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo
next-env.d.ts

.claude/settings.json
# claude code personal settings & secrets (DO NOT commit)
.claude/settings.local.json

# next.js upgrade backup
.upgrade-backup-*/

# frontend DB switch — local runtime files (real DSN/password).
# Committed safe templates are frontend/.env.staging.example & .env.production.example.
# (frontend/.env.local is already covered by the .env*.local rule above.)
frontend/.env.staging
frontend/.env.production
